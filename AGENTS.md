# AGENTS.md

## Project Overview

**Magazine** is a newly initialized repository. The project is in its early stages with no application code, build system, or dependencies established yet.

## Repository Structure

```
Magazine/
├── README.md        # Project readme
└── AGENTS.md        # This file - AI assistant guidelines
```

## Current State

- **Status**: Freshly initialized, no application code yet
- **Primary branch**: `main`
- **No build system, test framework, or linting configured**

## Development Guidelines

### Git Conventions

- Default branch: `main`
- Use descriptive commit messages in imperative mood (e.g., "Add feature X", not "Added feature X")
- Keep commits focused and atomic

### When Adding Code

As the project grows, follow these principles:

- Prefer simplicity over abstraction; avoid premature optimization
- Add a `.gitignore` appropriate for the chosen tech stack before committing generated/dependency files
- Set up linting and formatting early to maintain consistency
- Add tests alongside new functionality
- Document public APIs and non-obvious logic

## Commands

No build, test, or lint commands are configured yet. Update this section as tooling is added.
